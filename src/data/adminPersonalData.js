// Dummy personal profile for the demo admin (authData.js id "u-admin-1").
// Shaped like the future `profiles` table in database/schema.sql. Mirrors
// vendorPersonalData.js - admin has no store profile, just this.
const adminPersonalData = {
    name: "Admin User",
    email: "admin@shoppyspot.com",
    phone: "9000000000",
};

export default adminPersonalData;
